
describe('Minimal Application Build', () => {

    it('should return 401 on get auth status without logging in', () => {

        // const responseStatus: number = 200;
        // expect(responseStatus).toBe(401)
    })
});