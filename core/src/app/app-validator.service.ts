import { ApplicationSchema, ValidationError } from "@hals/common";

export const validateAppSchema = (schema: ApplicationSchema): ValidationError | null => {
   // implement validation
   return null;
};
